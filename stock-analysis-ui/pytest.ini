[pytest]
testpaths = src/tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: hits network (yfinance) and/or mutates a real local SQLite DB; excluded from CI by default, run manually with `pytest -m integration`
addopts = -ra
